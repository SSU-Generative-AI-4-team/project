# screens

확정된 고해상도 화면 디자인(최종 목업)을 보관합니다. `wireframes/`에서 검증된 레이아웃을 기반으로 실제 스타일, 색상, 타이포그래피를 적용한 결과물입니다.

## 네이밍 규칙

`[화면명].png` (버전 표시 없이 화면당 최신본 1개 유지, 예: `login.png`)

과거 버전이 필요하면 git 히스토리로 추적합니다. 같은 화면명으로 `wireframes/`의 초안, `decisions/`의 근거 문서와 짝을 이룹니다.
