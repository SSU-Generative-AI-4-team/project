# wireframes

저해상도 와이어프레임(초안)을 보관합니다. 레이아웃과 정보 구조를 빠르게 검증하는 단계로, 디테일한 스타일링은 다루지 않습니다.

## 네이밍 규칙

`[화면명]-v[버전].png` (예: `login-v1.png`, `login-v2.png`)

버전이 올라갈 때마다 파일을 새로 추가하고, 이전 버전은 삭제하지 않고 남겨 히스토리를 유지합니다. 최종 버전은 `screens/`로, 결정 배경은 `decisions/`로 이어집니다.
